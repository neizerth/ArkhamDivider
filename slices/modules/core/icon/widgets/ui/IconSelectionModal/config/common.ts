export const ICON_GROUP_SIZE = 53;
export const GROUP_HEADER_HEIGHT = 48;
export const LIST_GROUP_GAP = 16;
/** Extra px when scrolling to initial index so the section is clearly in view (covers estimate vs measured height). */
export const INITIAL_SCROLL_OFFSET_BUFFER_PX = 8;
/** Delay (ms) before first scroll attempt so dialog and virtualizer are ready. */
export const INITIAL_SCROLL_DELAY_MS = 150;
