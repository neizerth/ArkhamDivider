import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../model";

export default {
	"standalone-venice": {
		top: percent(-4),
		left: percent(3),
		scale: percent(90),
	},
	carnevale: {
		top: percent(-4),
		left: percent(4),
	},
} as IconPositionManifest;
