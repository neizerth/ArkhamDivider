import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../model";

export default {
	excelsior: {
		left: percent(-1),
		top: percent(-2),
		scale: percent(95),
	},
	alien_interference: {
		left: percent(-1),
		top: percent(-2),
	},
	dark_rituals: {
		top: percent(-3),
		left: percent(1),
	},
	excelsior_management: {
		left: percent(-1),
		top: percent(-2),
		scale: percent(80),
	},
	sins_of_the_past: {
		top: percent(1),
	},
	vile_experiments: {},
	murder_at_the_excelsior_hotel: {
		left: percent(-1),
		top: percent(-2),
		scale: percent(80),
	},
} as IconPositionManifest;
