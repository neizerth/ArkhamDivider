import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../model";

export default {
	blob_that_ate_everything_else: {
		top: percent(-4),
		left: percent(3),
	},
	migo_incursion_2: {
		top: percent(-2),
		left: percent(3),
	},
} as IconPositionManifest;
