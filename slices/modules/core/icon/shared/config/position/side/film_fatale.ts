import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../model";

export default {
	film_fatale: {
		top: percent(-1),
		left: percent(4),
	},
	cosmic_journey: {
		top: percent(-2),
		left: percent(3),
		scale: percent(90),
	},
	forgotten_island: {
		top: percent(-6),
		left: percent(6),
	},
	abominable_contessa: {
		top: percent(-8),
		left: percent(2),
		scale: percent(90),
	},
} as IconPositionManifest;
