import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../model";

export default {
	film_fatale: {
		top: percent(1),
	},
	cosmic_journey: {
		left: percent(-1),
		scale: percent(90),
	},
	forgotten_island: {
		top: percent(-4),
		left: percent(2),
	},
	abominable_contessa: {
		top: percent(-6),
		left: percent(-2),
		scale: percent(90),
	},
} as IconPositionManifest;
