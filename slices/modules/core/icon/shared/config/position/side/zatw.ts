import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../model";

export default {
	wendigo: {
		top: percent(-4),
		left: percent(3),
		scale: percent(95),
	},
	hannihah_valley: {
		top: percent(-4),
		left: percent(5),
	},
	wendigos_myth: {
		top: percent(-1),
		left: percent(3),
	},
} as IconPositionManifest;
