import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../model";

export default {
	roulette: {
		top: percent(-3),
		left: percent(3),
	},
	fortune_and_folly: {
		top: percent(-4),
		left: percent(5),
	},
	fortunes_chosen: {},
	plan_in_shambles: {
		top: percent(-2),
		left: percent(3),
	},
} as IconPositionManifest;
