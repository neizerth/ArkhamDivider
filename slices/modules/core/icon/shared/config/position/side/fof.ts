import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../model";

export default {
	roulette: {
		left: percent(-1),
		top: percent(-1),
	},
	fortune_and_folly: {
		top: percent(-2),
		left: percent(1),
	},
	fortunes_chosen: {},
	plan_in_shambles: {
		left: percent(-1),
	},
} as IconPositionManifest;
