import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../../model";

export default {
	zez: {
		top: percent(-2),
		scale: percent(90),
	},
} as IconPositionManifest;
