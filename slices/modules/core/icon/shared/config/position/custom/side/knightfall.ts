import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../../model";

export default {
	knightfall: {
		top: percent(-2),
		scale: percent(95),
	},
	"knightfall-fear_toxin": {
		top: percent(-4),
		left: percent(-2),
		scale: percent(95),
	},
} as IconPositionManifest;
