import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../../model";

export default {
	the_festival: {
		top: percent(-6),
		left: percent(3),
	},
	"the_festival-the_festival_set": {
		top: percent(-4),
		left: percent(2),
		scale: percent(90),
	},
} as IconPositionManifest;
