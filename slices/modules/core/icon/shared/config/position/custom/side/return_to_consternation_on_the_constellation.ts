import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../../model";

export default {
	return_to_consternation_on_the_constellation: {
		top: percent(-5),
	},
	"return_to_consternation_on_the_constellation-save_our_ship": {
		left: percent(-1),
		top: percent(-3),
		scale: percent(80),
	},
	"return_to_consternation_on_the_constellation-return_to_consternation_on_the_constellation_set":
		{
			top: percent(-4),
		},
} as IconPositionManifest;
