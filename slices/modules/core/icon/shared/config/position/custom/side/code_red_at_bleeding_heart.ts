import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../../model";

export default {
	code_red_at_bleeding_heart: {
		left: percent(-1),
		scale: percent(80),
	},
	"code_red_at_bleeding_heart-graveyard_shift": {
		top: percent(-1),
		scale: percent(85),
	},
	"code_red_at_bleeding_heart-code_red_at_bleeding_heart_set": {
		top: percent(-2),
		scale: percent(110),
	},
} as IconPositionManifest;
