import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../../model";

export default {
	wendigo: {
		left: percent(-1),
		top: percent(-2),
		scale: percent(95),
	},
	hannihah_valley: {
		top: percent(-2),
		left: percent(1),
	},
	wendigos_myth: {
		left: percent(-1),
		top: percent(1),
	},
} as IconPositionManifest;
