import { percent } from "@/shared/util";
import type { IconPositionManifest } from "../../../../model";

export default {
	zdms: {
		top: percent(-6),
		left: percent(4),
		scale: percent(110),
	},
	"zdms-cards": {
		top: percent(-4),
		left: percent(3),
	},
} as IconPositionManifest;
