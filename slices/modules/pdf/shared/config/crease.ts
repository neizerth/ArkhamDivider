import { rgb } from "@/modules/core/color/shared/lib";

/** Crease line color (RGB) */
export const CREASE_COLOR = rgb(0, 0, 255);

/** Line width for crease, in pt (same as lasercut) */
export const CREASE_LINE_WIDTH = 0.01;
