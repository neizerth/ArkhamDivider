import { cmyk } from "@/modules/core/color/shared/lib";

export const blackInk = cmyk(0, 0, 0, 100);

export const xpFontFamily = "ArnoPro";

/**
 * Vertical nudge matching scenario number field CSS: `translateY(0.05em)` (fraction of font size).
 */
export const SCENARIO_NUMBER_VERTICAL_TRIM = 0.05;
