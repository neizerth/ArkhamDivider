import Box from "@mui/material/Box";
import type { Divider } from "@/modules/divider/shared/model";

type DividerViewProps = Divider;

export function DividerView(_: DividerViewProps) {
	return <Box />;
}
