pre-commit:
  parallel: true
  commands:
    format:
      files: git diff --cached --name-only
      glob: "*.{js,ts,jsx,tsx}"
      run: npm run format
    types:
      files: git diff --cached --name-only
      glob: "*.{js,ts,jsx,tsx}"
      run: npm run typecheck
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit