pre-commit:
  parallel: true
  commands:
    format:
      files: git diff --name-only @{push}
      glob: "*.{js,ts,jsx,tsx}"
      run: npm run format
    types:
      files: git diff --name-only @{push}
      glob: "*.{js,ts, jsx, tsx}"
      run: npm run typecheck
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit